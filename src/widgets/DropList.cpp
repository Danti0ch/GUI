#include "DropList.h"


/*
DropList::DropList(Vector size, RectButton* pControlButton):
    VListWidget(size),
    pControlButton_(pControlButton)
{
    pControlButton->addHandler(new ObjAction<DropList>(this, &DropList::invertVisible));
}
*/