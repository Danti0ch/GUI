#include "geometry_objects.h"

INTERSECTION_MOD Ray::GetRaysIntersection(const Ray& ray2, std::Vector& storage){
    
}
//----------------------------------------------------------------------------------------//

INTERSECTION_MOD Ray::GetRayPlaneInterSection(const Plane& plane, std::Vector& storage);
INTERSECTION_MOD Ray::GetRayPolylineInterSection(const Plane* planes, std::Vector& storage);
